# ATENÇÃO: este arquivo contém segredos e NÃO deve ser versionado (já está no .gitignore).
#
# IMPORTANTE - AÇÃO NECESSÁRIA:
# As senhas de banco (DB_DW_PASSWORD, DB_APP_PASSWORD) e a chave do Gemini (GEMINI_API_KEY)
# abaixo são as MESMAS que já estavam expostas em texto puro no código-fonte
# (database.js e frontend/js/analytics_ia.js). Elas só foram movidas para cá.
# Como já estiveram expostas, o ideal é ROTACIONAR (trocar) esses valores assim que possível:
#   - Senha do MySQL local (usuário root)
#   - Senha do usuário 'cezanilton' no PostgreSQL (192.168.70.6)
#   - Chave da API do Gemini (revogar em https://aistudio.google.com/apikey e gerar outra)
# Depois de trocar, basta atualizar os valores abaixo.

# --- JWT ---
# Chave nova, gerada aleatoriamente (64 bytes). Trocar esta chave invalida
# todas as sessões ativas no momento da troca - isso é esperado e desejável.
JWT_SECRET=845d5087b5514f6cbc45c7cb92d879ab5eeaade23654db2736b8338c761f83e7a1d8e8fbd49a0b3fa9b84a5a968686135ca3ccee0da25cb42f37a0bc1d4cf2b2

# --- Banco de dados: Data Warehouse (PostgreSQL, remoto) ---
DB_DW_USER=peterson_freire
DB_DW_HOST=192.168.70.6
DB_DW_NAME=dw_tijuca_v2
DB_DW_PASSWORD=Tijuca@2025
DB_DW_PORT=5432

# --- Banco de dados: Aplicação (MySQL, local) ---
DB_APP_HOST=localhost
DB_APP_USER=root
DB_APP_PASSWORD=root
DB_APP_NAME=folha_pagamentos
DB_APP_PORT=3306

# --- E-mail (recuperação de senha) ---
# Estes ainda são os valores de exemplo originais - preencha com credenciais reais
# (ideal: uma "senha de app" do Gmail, não a senha normal da conta).
EMAIL_USER=peterson@tijucaalimentos.com
EMAIL_PASS=bdwgwcweutojlewa

# --- IA (Google Gemini) ---
# Movida do frontend para cá - agora só o backend tem acesso a esta chave.
GEMINI_API_KEY=AIzaSyBrMxgY9I_7lmfEBdk6lMbPHM-i-bb-0Eo

# --- Servidor ---
PORT=8010
# Origem(ns) permitida(s) para CORS, separadas por vírgula (ex.: http://192.168.0.157:8003)
CORS_ORIGIN=*

# --- Movimentações de Setor (e-mail mensal aos gestores) ---
# Endereço que aparece no link do e-mail enviado ao gestor - precisa ser algo
# que ELE consiga abrir do computador dele (por isso o IP da rede, não localhost).
APP_BASE_URL=http://192.168.0.157:8010
# Dia do mês em que o disparo automático mensal roda (olha pro mês anterior).
MOVIMENTACAO_DISPATCH_DAY=5
# Depois de quantos dias o link de resposta do gestor expira.
MOVIMENTACAO_TOKEN_EXPIRA_DIAS=60
